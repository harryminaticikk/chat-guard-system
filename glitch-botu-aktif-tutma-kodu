const app = express();
app.get("/", (request, response) => {
  console.log(Date.now() + " Ping tamamdır.");
  response.sendStatus(200);
});
app.listen(process.env.PORT);
setInterval(() => {
  http.get(`http://${process.env.PROJECT_DOMAIN}.glitch.me/`);
}, 28000);


 ⛔ Önemli: Buradaki kodları ana dosyanız içerisinde "const" ile başlıyan ilk kod satırlarının bittiği kısmın altına ekleyin yoksa çalışmaz.
